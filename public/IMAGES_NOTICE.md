# Note about image files

I updated the Signature Collection component to use the image files you uploaded to the repository root (named "image 1.jpg" through "image 4.jpg"). Vercel will pick up the change and redeploy automatically.

If you prefer cleaner organization, you can move these files into a `public/images/` folder using the GitHub web UI:
1. Open the repository on GitHub.
2. Click the file (e.g., `image 1.jpg`) → click the pencil (edit) icon → choose "Replace" to upload a new file in the `public/images/` path, or use Add file → Upload files to add the images to `public/images/` and then delete the originals from the root.

If you want me to rename/move the files for you (so they live in `public/images/` with names `bottle1.jpg`...`bottle4.jpg`), paste public URLs for each image and I will fetch, commit into `public/images/`, and remove or leave the originals as you prefer.
